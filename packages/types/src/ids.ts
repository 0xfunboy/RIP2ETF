/**
 * Shared identifier aliases for ElizaOS packages.
 * Templates types cause DTS issues, so we centralize these as simple strings.
 */
export type UUID = string;
export type ServerId = string;
export type EntityId = UUID;
